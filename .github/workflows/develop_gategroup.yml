name: Build and Deploy FastAPI Docker App to Azure App Service

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to Azure Container Registry (ACR)
        run: |
          echo "${{ secrets.ACR_PASSWORD }}" | docker login myregistry123.azurecr.io \
            -u ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Build and push Docker image to ACR
        run: |
          docker build -t myregistry123.azurecr.io/fastapi-app:latest .
          docker push myregistry123.azurecr.io/fastapi-app:latest

      - name: Deploy to Azure Web App (Linux Container)
        uses: azure/webapps-deploy@v2
        with:
          app-name: gategroup # 🔁 Replace with your App Service name
          slot-name: Production
          images: myregistry123.azurecr.io/fastapi-app:latest
